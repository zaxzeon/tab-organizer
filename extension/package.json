{
    "name": "vivaldi-tab-organizer-extension",
    "version": "0.1.0",
    "private": true,
    "type": "module",
    "scripts": {
        "dev": "vite",
        "build:wasm": "make -C ../wasm build",
        "build:ext": "vite build",
        "build": "npm run build:wasm && npm run build:ext",
        "preview": "vite preview",
        "test": "vitest run"
    },
    "devDependencies": {
        "@crxjs/vite-plugin": "2.2.0",
        "@types/chrome": "^0.0.258",
        "@types/node": "^22.7.5",
        "typescript": "^5.6.3",
        "vite": "^5.4.8",
        "vitest": "^2.1.1"
    }
}